# Copy this file to `.env` (in supabase/functions/) and fill in a real key.
# This file is gitignored — never commit the real secret.
# Anthropic API key used by the edge functions. Get one from the Anthropic console.
EXPO_PUBLIC_ANTHROPIC_API_KEY=sk-ant-apiXX-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
